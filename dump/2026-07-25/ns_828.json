{"batchcomplete":true,"query":{"pages":[{"pageid":37,"ns":828,"title":"Module:Source","revisions":[{"revid":75,"parentid":47,"timestamp":"2026-03-20T22:37:34Z","slots":{"main":{"contentmodel":"Scribunto","contentformat":"text/plain","content":"local p = {}\n\nfunction p.get(frame)\n    local title = frame.args[1]\n    local page = mw.title.new(title)\n    if not page then return \"Page non trouvée\" end\n    local content = page:getContent()\n    if not content then return \"Contenu vide\" end\n    -- On enveloppe dans nowiki pour éviter que le wiki n'interprète les balises includeonly\n    return frame:preprocess(\"<nowiki>\" .. content .. \"</nowiki>\")\nend\n\nreturn p"}}}]},{"pageid":38,"ns":828,"title":"Module:Source/doc","revisions":[{"revid":48,"parentid":0,"timestamp":"2026-03-20T20:36:50Z","slots":{"main":{"contentmodel":"wikitext","contentformat":"text/x-wiki","content":"=== Présentation (Overview) ===\nCe module permet d'extraire et d'afficher le code source brut d'une page (modèle, formulaire ou autre) sans que le wiki ne l'interprète. Il est utilisé pour automatiser la page de [[Récapitulatif technique]].\n\n''This module extracts and displays the raw source code of any page (template, form, etc.) without interpretation. It is used to automate the [[Technical Summary]] page.''\n\n=== Utilisation (Usage) ===\nLa syntaxe de base consiste à utiliser la fonction `get` via un `invoke`. Il est recommandé de l'envelopper dans une balise `<pre>` ou un tag `#tag:pre` pour conserver la mise en forme.\n\n''The basic syntax uses the `get` function. Wrapping it in a `<pre>` tag or `#tag:pre` is recommended to preserve formatting.''\n\n==== Syntaxe (Syntax) ====\n<pre>\n{{#tag:pre|{{#invoke:Source|get|Nom de la page}}}}\n</pre>\n\n==== Paramètres (Parameters) ====\n* '''1 :''' Le nom complet de la page, incluant l'espace de noms (ex: `Template:Referenced item`).\n* '''''1:''' The full name of the page, including the namespace (e.g., `Template:Referenced item`).''\n\n=== Exemple (Example) ===\nPour afficher le code de ce module lui-même :\n''To display the code of this module itself:''\n<pre>\n{{#tag:pre|{{#invoke:Source|get|Module:Source}}}}\n</pre>\n\n[[Catégorie:Modules interne SGDT]]"}}}]},{"pageid":103,"ns":828,"title":"Module:Base36","revisions":[{"revid":237,"parentid":235,"timestamp":"2026-04-07T16:15:52Z","slots":{"main":{"contentmodel":"Scribunto","contentformat":"text/plain","content":"local p = {}\n\nfunction p.next(frame)\n    -- Récupère l'entrée (SMW)\n    local raw_input = frame.args[1] or frame:getParent().args[1] or \"\"\n    \n    -- NETTOYAGE CHIRURGICAL :\n    -- 1. On enlève les \"strip markers\" (codes internes de MediaWiki)\n    local clean = mw.text.unstrip(raw_input)\n    -- 2. On enlève les balises HTML (ex: <span>...</span>)\n    clean = clean:gsub(\"<[^>]+>\", \"\")\n    -- 3. On enlève tout ce qui n'est pas un chiffre ou une lettre\n    clean = clean:match(\"[%w]+\") or \"\"\n    -- 4. On met en majuscules et on enlève les espaces\n    clean = mw.text.trim(clean):upper()\n    \n    -- Si après nettoyage c'est vide, on commence à 0001\n    if clean == \"\" then return \"0001\" end\n    \n    -- Conversion Base 36 -> Décimal\n    local dec = tonumber(clean, 36)\n    \n    -- DEBUG : Si la conversion échoue encore, on affiche ce que Lua a \"vu\"\n    if not dec then return \"ERR_LUA_VOIT:\" .. clean end\n    \n    -- Incrémentation\n    dec = dec + 1\n    \n    -- Re-conversion en Base 36\n    local chars = '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ'\n    local base36 = ''\n    local temp = dec\n    while temp > 0 do\n        local mod = temp % 36\n        base36 = string.sub(chars, mod + 1, mod + 1) .. base36\n        temp = math.floor(temp / 36)\n    end\n    \n    -- Formatage sur 4 caractères\n    while string.len(base36) < 4 do\n        base36 = '0' .. base36\n    end\n    \n    return base36\nend\n\n-- Détecteur de trous (Gaps) dans la numérotation\nfunction p.findGaps(frame)\n    local raw_input = frame.args[1] or frame:getParent().args[1] or \"\"\n    if raw_input == \"\" then return \"Aucune donnée trouvée.\" end\n\n    local existing_refs = {}\n    local max_dec = 0\n\n    -- 1. On lit toutes les références séparées par des virgules\n    for ref in string.gmatch(raw_input, \"([^,]+)\") do\n        local clean_ref = mw.text.unstrip(ref)\n        clean_ref = clean_ref:gsub(\"<[^>]+>\", \"\")\n        clean_ref = mw.text.trim(clean_ref):upper()\n        clean_ref = clean_ref:match(\"[%w]+\")\n        \n        if clean_ref and clean_ref ~= \"\" then\n            local dec = tonumber(clean_ref, 36)\n            if dec then\n                -- On stocke la référence trouvée\n                existing_refs[dec] = true\n                -- On met à jour le plafond si nécessaire\n                if dec > max_dec then max_dec = dec end\n            end\n        end\n    end\n\n    if max_dec == 0 then return \"Aucune référence valide analysée.\" end\n\n    local missing = {}\n    local chars = '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ'\n    \n    -- 2. On compte de 1 jusqu'au plafond et on note les absents\n    for i = 1, max_dec do\n        if not existing_refs[i] then\n            local base36 = ''\n            local temp = i\n            while temp > 0 do\n                local mod = temp % 36\n                base36 = string.sub(chars, mod + 1, mod + 1) .. base36\n                temp = math.floor(temp / 36)\n            end\n            while string.len(base36) < 4 do base36 = '0' .. base36 end\n            table.insert(missing, base36)\n        end\n    end\n\n    -- 3. Affichage du résultat\n    if #missing == 0 then\n        return \"Aucun trou détecté dans la séquence (jusqu'à \" .. string.format(\"%04s\", max_dec) .. \").\"\n    else\n        return table.concat(missing, \", \")\n    end\nend\n\nreturn p"}}}]}]}}