# Rapport de session — 19 août 2026

**Exécuté le :** 19 août 2026, session Claude Code, compte `Cywil`.

Session en trois volets : correctif de tri sur `Modèle:Lieu`, reconnaissance
en lecture seule préparatoire à la tâche 3 du lot 10. (Le solde de la tâche 2
du lot 10 — modification de `Modèle:Functional item` et réécriture des cinq
pages de procédé — a été exécuté plus tôt dans cette même session et est déjà
consigné dans `lot-10-tache2-rapport.md` §6 ; non repris ici en détail.)

Reprise après interruption dans la même journée : mesure de ce qui est
réellement accepté au stockage par les propriétés de type Number, après
passage du séparateur décimal à la virgule (`smw_decseparator`, changement
consigné hors de ce rapport). Volet ajouté en §1.2, §2.4 et §2.5.

---

## 1. Écritures sur le wiki

### 1.1 `Modèle:Lieu` — tri alphabétique

- **Méthode :** `bin/wiki-put.sh` (édition standard, page déjà existante,
  `protection: []`).
- **Résultat API :** `pageid: 289`, `oldrevid: 779`, `newrevid: 801`,
  `result: Success`.
- **Résumé d'édition exact :**
  `[Correctif] Modèle:Lieu — tri alphabétique, désormais permis par la collation`
- **Diff exact (une seule ligne, reste strictement identique) :**

```diff
  |?Specimen_status = État
  |format=table
- |sort=Inventory_number
+ |sort=
  |order=asc
  |limit=200
```

  Compteur, `|limit=200`, `|order=asc` et la colonne Rang laissés inchangés,
  comme demandé. L'espace en début de ligne de paramètre est conservé.

- **Contrôles post-écriture exécutés :**
  - `list=categorymembers` sur `Catégorie:Lieu` → trois pages
    (`Jardin de Chilhac`, `Le Buisson de Cerzat`, `Terrasse de Chilhac`),
    toutes purgées (`bin/wiki-purge.sh`, `purged: true` sur les trois).
  - Rendu réel (`action=parse&page=Le Buisson de Cerzat&prop=text`, pas de
    prévisualisation) :
    - Compteur : **29** item(s) physique(s) — conforme.
    - Tableau : **29** lignes de données comptées programmatiquement
      (`data-row-number` de 1 à 29) — conforme.
    - Les trois « Ail éléphant » (ECL-0003, ECL-0041, ECL-0042) occupent les
      lignes 1 à 3, groupées en tête — conforme.
    - `Égopode — Le Buisson de Cerzat (ECL-0013)` en ligne 12, entre
      `Crosnes du Japon` (11) et `Fraisier musqué` (13) — position
      alphabétique correcte pour un É traité comme un E par la collation.
    - `Hémérocalle — Le Buisson de Cerzat (ECL-0021)` en ligne 16, entre
      `Helianthi` (15) et `Hysope` (17) — position alphabétique correcte,
      même vérification.
  - Ordre complet des 29 lignes relevé et contrôlé ligne à ligne, aucune
    anomalie.

### 1.2 `Utilisateur:Cywil/Bac à sable` — test de stockage décimal (deux écritures)

**Écriture 1 — dépôt du test.**
- **Méthode :** `bin/wiki-put.sh` (page déjà existante, contenu préalablement
  relu).
- **Résultat API :** `pageid: 110`, `oldrevid: 742`, `newrevid: 812`,
  `result: Success`.
- **Résumé d'édition exact :**
  `[Correctif] Utilisateur:Cywil/Bac à sable — test stockage décimal point vs virgule sur propriétés Number`
- **Contenu exact écrit** (remplace la note de sujet précédente) :
  ```
  [[Max_head::1.5]]
  [[Power_rating::1,5]]
  ```
- **Contrôles post-écriture exécutés :**
  - `bin/wiki-purge.sh` sur la page → `purged: true`.
  - `bin/wiki-api.sh --facts` puis `action=browsebysubject` en JSON brut sur
    la page :
    - `Power_rating -> ['1.5']` — la forme virgule (`1,5`) est **stockée
      correctement**, en interne sous forme pointée (`1.5`).
    - `Max_head` **absent** des faits directs. À la place, une clé `_ERRC`
      pointe vers un sous-objet d'erreur portant :
      - `_ERRP -> ['Max_head#102##']`
      - `_ERRT -> ['[2,"smw-datavalue-number-textnotallowed",".5","1"]']`

      La forme point (`1.5`) est donc **rejetée à l'enregistrement**, pas
      corrompue en une autre valeur : SMW lit `1` puis bute sur le `.5`
      restant, qu'il traite comme du texte parasite (le séparateur attendu
      étant la virgule). Aucune valeur fausse silencieuse observée dans ce
      test — soit rejet explicite avec erreur, soit stockage correct.

**Écriture 2 — reblanchiment.**
- **Méthode :** `bin/wiki-put.sh`.
- **Résultat API :** `pageid: 110`, `oldrevid: 812`, `newrevid: 813`,
  `result: Success`.
- **Résumé d'édition exact :**
  `[Correctif] Utilisateur:Cywil/Bac à sable — reblanchi après test décimal`
- **Contenu exact écrit** (retour au contenu d'avant le test) :
  ```
  Page bac à sable — sert aux tests du lot 9 (voir CLAUDE.md, lot-9-cadrage-plantes.md).
  ```
- **Contrôles post-écriture exécutés :** `bin/wiki-purge.sh` sur la page →
  `purged: true`. Faits SMW non relus après ce second purge (aucun contenu
  annotant restant à vérifier, la page ne portant plus de balise `[[…::…]]`).

---

## 2. Résultats de reconnaissance

### 2.1 Les six propriétés du §1 de `lot-10-tache3-proposition.md`

Relevé par `bin/wiki-api.sh --facts` sur chaque page `Attribut:`, en lecture
seule, aucune écriture :

| Propriété | Existe ? | `Has type` (`_TYPE`) | `Property_cardinality` | `Property_domain` | `Property_range` |
|---|---|---|---|---|---|
| `Manufacturer` | oui (pageid 289 côté ns102, cf. §2.2) | Page (`_wpg`) | single | `Referenced_item` | « page libre — aucune catégorie Fabricant dédiée pour l'instant » |
| `Manufacturer_reference` | oui | Code (`_cod`) | single | `Referenced_item` | « référence catalogue chez Manufacturer » |
| `Supplier` | oui | Page (`_wpg`) | single | `Referenced_item` | « page libre — aucune catégorie Fournisseur dédiée pour l'instant » |
| `Supplier_reference` | oui | Code (`_cod`) | single | `Referenced_item` | « référence catalogue chez Supplier » |
| `Serial_number` | oui | Keyword (`_keyw`) | single | **`Physical_item`** (pas `Referenced_item`) | « chaîne courte » |
| `Procurement_route` | **non** | — | — | — | — |

**Écart avec le §1 de la proposition.** Le fichier de tâche 3 affirme
« `Procurement_route` existe depuis le lot 9 — couvre le banc
auto-construit ». C'est faux sur le wiki vivant : `action=query&titles=…`
renvoie `missing: true`, et une recherche plein texte sur « Procurement »
dans l'espace `Attribut:` ne retourne aucun résultat. Un balayage complet de
l'espace `Attribut:` (90 pages au total, liste exhaustive obtenue via
`list=allpages&apnamespace=102`) ne montre aucune propriété au nom voisin —
la plus proche par le sens est `Sourcing_year` (année d'obtention du lot,
`Has type: Number`, `Property_domain: Referenced_item`), mais elle porte une
**année**, pas une **route d'acquisition** ; elle ne peut pas s'y substituer
sans perte d'information. La cinquième propriété que la tâche 3 comptait
comme déjà acquise ne l'est donc pas — la confrontation du §1 doit être
refaite en tenant compte de ce manque avant toute écriture.

### 2.2 Absence de collision pour les quatre propriétés à créer

Les 90 noms de l'espace `Attribut:` (liste complète obtenue) ne contiennent
aucune propriété nommée `Power_rating`, `Max_thickness`, `Materials_worked`
ou `Measured_quantities`, ni de nom voisin qui recouvrirait le même besoin :
aucune propriété de puissance, d'épaisseur, de matériau travaillé ou de
grandeur mesurée n'existe actuellement. Les quatre créations proposées ne
percutent rien d'existant.

### 2.3 Câblage de `Modèle:Referenced item` et `Formulaire:Referenced item`

Wikitexte des deux pages relu en direct (pas depuis une copie locale du
dépôt, qui datait d'avant le 15 août et ne portait pas encore
`Sourcing_year`).

**Câblés pour les cinq propriétés existantes citées au §1 de la tâche 3,**
à l'exception de `Serial_number` (hors périmètre : son domaine est
`Physical_item`, pas `Referenced_item`, donc porté par `Modèle:Physical item`
et non par ce modèle) :
- `Modèle:Referenced item` : `#set` porte `Manufacturer`,
  `Manufacturer_reference`, `Supplier`, `Supplier_reference`, et aussi
  `Sourcing_year` (non demandé mais constaté) ; chacune a sa ligne
  d'affichage dans le tableau.
- `Formulaire:Referenced item` : un champ `{{{field|…}}}` par propriété,
  même liste, y compris `Sourcing_year` avec une infobulle `{{#info:}}`.

`Procurement_route` n'apparaît dans aucun des deux, cohérent avec son
absence constatée en §2.1.

**Ce qu'il faudrait ajouter pour les quatre nouvelles propriétés,** si la
tâche 3 les crée telles que proposées :
- `Modèle:Referenced item`, dans le bloc `#set` : `Power_rating`,
  `Max_thickness`, `Materials_worked` (avec `+sep=,`, `Has type: Page` —
  absorbe l'espace de tête par normalisation de titre, pas de piège de
  troncature comme rencontré sur `Practice_domain`) et
  `Measured_quantities` (`Has type: Text`, multivaluée : le même piège que
  `Practice_domain` en tâche 2 s'appliquerait — `+sep=,` seul stockerait un
  espace de tête sur les valeurs après la première ; il faudrait le même
  contournement par `#arraymap` + `#set` par valeur). Plus une ligne
  d'affichage par propriété dans le tableau.
- `Formulaire:Referenced item` : quatre champs supplémentaires — `text` pour
  `Power_rating` et `Max_thickness` (type `Number`), `tokens` pour
  `Materials_worked` (type `Page`, valeurs libres tant qu'aucune catégorie
  de matériaux n'existe) et pour `Measured_quantities` (type `Text`,
  vocabulaire ouvert).

Aucune modification appliquée : `Modèle:Referenced item` et
`Formulaire:Referenced item` sont des pages en service, hors du garde-fou 6
levé pour cette session (qui ne couvrait que `Modèle:Functional item`). Le
diff est à soumettre à Cyril avant toute écriture, conformément à la
consigne.

### 2.4 État local du dépôt (`travaux/` et `git status`)

Relevé sur le poste local, avant toute correction :
- `ls travaux/ | wc -l` → **61** entrées.
- `git status` → branche `main`, « à jour avec `origin/main` », rien à
  valider (copie de travail propre).

Cyril indique que le dépôt distant contient 60 fichiers dans `travaux/` et
qu'il n'en voit aucun de son côté. Le poste local, lui, voit 61 entrées et un
état git propre et synchronisé — pas de désynchronisation locale visible
entre l'index git et le disque. Aucune investigation plus poussée menée à ce
stade (consigne explicite : ne rien corriger, ne rien supprimer, ne relancer
aucune synchronisation).

### 2.5 Propriétés de type Number existantes et valeurs décimales déjà saisies

Liste complète obtenue par `action=ask` avec la condition
`[[Has type::Number]]` : **14 propriétés** au total — `Adult_height`,
`Adult_width`, `Hardiness_min_temp`, `Latitude`, `Longitude`, `Max_head`,
`Max_thickness`, `Nominal_diameter`, `Planted_count`, `Planting_rank`,
`Power_rating`, `Secondary_diameter`, `Sourcing_year`, `Time_to_production`.

Pour chacune, requête `[[Propriété::+]]|?Propriété|limit=500` en lecture
seule :

- **Dix propriétés sans aucune valeur saisie nulle part**
  (`"results": []` dans la réponse JSON brute) : `Adult_height`,
  `Adult_width`, `Hardiness_min_temp`, `Max_head`, `Max_thickness`,
  `Nominal_diameter`, `Planting_rank`, `Power_rating`, `Secondary_diameter`,
  `Time_to_production`. (`Max_head` et `Power_rating` portaient une valeur de
  test au moment de la requête intermédiaire, retirée par le reblanchiment du
  §1.2 — la requête ci-dessus est celle exécutée après reblanchiment.)
- **`Planted_count`** — 4 pages, valeurs `1`, `3`, `1`, `1`. Aucune décimale.
- **`Sourcing_year`** — 28 pages, valeurs toutes entières (années :
  2020, 2023, 2024, 2025, 2026). Aucune décimale.
- **`Latitude`** — 3 pages : `Jardin de Chilhac` (45.15504),
  `Le Buisson de Cerzat` (45.17142), `Terrasse de Chilhac` (45.155265).
  **Valeurs décimales, présentes.**
- **`Longitude`** — mêmes 3 pages : 3.437188, 3.488276, 3.437144.
  **Valeurs décimales, présentes.**

**Vérification de la saisie source** pour `Latitude`/`Longitude`, par lecture
directe du wikitexte des trois pages (pas seulement de la valeur stockée) :

| Page | Wikitexte source | Valeur stockée (`browsebysubject`) |
|---|---|---|
| `Jardin de Chilhac` | `Latitude=45,155040` / `Longitude=3,437188` | `45.15504` / `3.437188` |
| `Le Buisson de Cerzat` | `Latitude=45,171420` / `Longitude=3,488276` | `45.17142` / `3.488276` |
| `Terrasse de Chilhac` | `Latitude=45,155265` / `Longitude=3,437144` | `45.155265` / `3.437144` |

Les trois pages sont saisies avec la virgule. Les valeurs stockées
correspondent exactement à la valeur saisie (le zéro de fin est retiré,
aucun autre écart constaté) — cohérent avec le comportement observé en §1.2
sur `Power_rating`.

---

## 3. Ce qui a échoué ou n'a pas pu être obtenu

**Rien n'a échoué à l'écriture** : l'unique écriture de cette partie de
session (`Modèle:Lieu`, revid 801) a été acceptée du premier coup, et tous
les contrôles post-écriture demandés sont conformes.

**Une hypothèse du document de cadrage de la tâche 3 s'est révélée fausse à
l'usage** : `Procurement_route` n'existe pas sur le wiki vivant, contrairement
à ce qu'affirme le §1 de `lot-10-tache3-proposition.md` (« existe depuis le
lot 9 »). Voir §2.1 pour le détail de la vérification et l'absence de
substitut exact. C'est exactement le risque que l'étape de reconnaissance
préalable visait à couvrir avant d'écrire quoi que ce soit pour la tâche 3.

**Rien n'a échoué à l'écriture non plus dans le volet décimal** : les deux
écritures du §1.2 (dépôt du test, reblanchiment) ont chacune été acceptées
du premier coup. Le rejet de `Max_head::1.5` documenté au §1.2 n'est pas un
échec de script ou de méthode : c'est le résultat même que le test cherchait
à mesurer — la forme point est refusée à l'enregistrement par SMW avec
`smw-datavalue-number-textnotallowed`, elle n'aboutit jamais à une valeur
stockée fausse.

**Point resté hors de portée** : l'écart entre le compte local de
`travaux/` (61 entrées, §2.4) et ce que voit Cyril côté téléphone (aucun
fichier), alors que le dépôt distant en compte 60. Consigne explicite de ne
pas creuser plus loin dans cette session (ne rien corriger, ne rien
supprimer, ne relancer aucune synchronisation) : le §2.4 se limite donc au
relevé brut, sans diagnostic ni hypothèse sur la cause.
