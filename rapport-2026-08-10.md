# Rapport de session — Lot 6 (clôture Tâche 2, ouverture Tâche 4a)

**Exécuté le :** 10 août 2026, session Claude Code, compte `Cywil@claude-sgdt`.

La session couvre trois phases :
- **Phase A** — contrôle post-écriture de la partie 2C (Tâche 2) : comparaison
  `browsebysubject` avant/après sur `Bidon 220L bleu plastique Borde`, écart
  constaté et expliqué par l'utilisateur, Tâche 2 close.
- **Phase B** — mise à jour du statut des quatre fichiers `diffs/*.md` déjà
  appliqués sur le wiki (édition locale, pas d'écriture wiki).
- **Phase C** — lecture de `lot-6-suite.md`, exécution de la Tâche 4a
  (enquête sur l'espace résiduel du widget `tokens`) : test de la piste 1
  (`#arraymap`) dans le bac à sable, validée dès le premier essai.

---

## 1. Écritures sur le wiki

Une seule écriture wiki cette session.

### 1.1 `Utilisateur:Cywil/Bac à sable` (Phase C, Tâche 4a)

- **Méthode :** `bin/wiki-put.sh` (page existante, non protégée — contrôle
  `--protection` fait avant écriture, voir §2.4 ; état lu avant écriture,
  page vide, voir §2.3). `bin/wiki-login.sh` relancé juste avant (`Success
  Cywil`) conformément à la convention de session (la session peut avoir
  expiré entre lecture et écriture).
- **Résultat API :** `result: Success`, `pageid: 110`, `oldrevid: 247`,
  `newrevid: 328`.
- **Résumé d'édition exact :**
  `[Lot 6][Tâche 4a] Bac à sable — test piste 1 : #arraymap comme normaliseur de l'espace après délimiteur`
- **Contenu exact écrit** (page vide auparavant, donc pas de diff — contenu
  intégral) :
```
Test 4a — Piste 1 (#arraymap comme normaliseur de l'espace après délimiteur) :
{{#arraymap:26x34, 20x27|,|@@@|@@@|,}}
```
- **Contrôle post-écriture :** rendu HTML récupéré (`action=parse&prop=text`,
  hors périmètre de `wiki-get.sh` qui ne gère que `prop=wikitext`, donc appel
  curl direct avec les cookies de session). Texte extrait :
  `26x34,20x27` — espace après le délimiteur bien rogné, virgule de sortie
  sans espace. Résultat conforme à l'hypothèse de la piste 1 dès le premier
  essai ; pistes 2 et 3 non testées (voir §2.5 et §3).

---

## 2. Résultats de reconnaissance

### 2.1 Contrôle `browsebysubject` avant/après sur la partie 2C (Phase A)

Lecture du wikitexte courant de `Bidon 220L bleu plastique Borde`
(`bin/wiki-get.sh`) : confirme les paramètres déjà renommés
(`Corresponds_to_organic`, `Maturity_level`), plus un `Item_description`
renseigné.

Appel direct `action=browsebysubject` (curl, `wiki-get.sh` ne gère pas cette
action) sur la même page. Faits utilisateur obtenus :
- `Item_ref = 000N`
- `Maturity_level = Certified`
- `Corresponds_to_organic = Bidon_220L#0##`
- `Item_description = "Contenant de 220 litres en plastique bleu avec ouverture intégrale par un couvercle noir"`

Comparaison avec le snapshot avant écriture consigné dans
`diffs/2026-08-10-tache2-2c.md` (capturé le même jour, avant les trois
écritures de la Tâche 2) : les trois premiers faits sont strictement
identiques. Le quatrième (`Item_description`) est absent du snapshot avant
écriture et absent du diff validé en 3.1 de ce même fichier (qui ne touchait
que `Item_ref`, `organic_link→Corresponds_to_organic`,
`maturity→Maturity_level`). Signalé à l'utilisateur comme écart ; expliqué
par lui comme une resaisie manuelle via le formulaire lors de l'étape humaine
de vérification prévue par le cadrage — pas un effet du renommage. Tâche 2
close sur cette base.

### 2.2 Vérification du statut réel des quatre écritures documentées dans `diffs/`

Avant de corriger les en-têtes « Statut » des quatre fichiers (tous encore
marqués « proposé, non validé, non écrit »), lecture en direct des pages
concernées pour confirmer qu'elles portent bien les changements attendus,
plutôt que de se fier aux fichiers locaux seuls :
- `Modèle:Functional item` — `+sep=,` présent, affichage `#arraymap`,
  libellé « Fonctions parentes » au pluriel. Conforme à
  `diffs/2026-08-09-tache1.md`.
- `Formulaire:Functional item` — champ `Part_of` en
  `list|delimiter=,|input type=tokens`. Conforme à
  `diffs/2026-08-09-tache1-etape2.md`.
- `Modèle:Referenced item` — affichage `#arraymap` sur les parents, ligne
  « Exemplaires physiques » présente, paramètres renommés
  (`Item_description`, `Maturity_level`, `Part_of`,
  `Corresponds_to_organic`). Conforme à `diffs/2026-08-09-tache2.md` (2A+2B)
  et `diffs/2026-08-10-tache2-2c.md` (renommage).
- `Formulaire:Referenced item` — champs alignés sur les noms de propriétés.
  Conforme à `diffs/2026-08-10-tache2-2c.md`.
- `Modèle:Referenced item/doc` — paramètres `Maturity_level` et `Part_of` à
  jour dans la prose. Conforme à `diffs/2026-08-10-tache2-2c.md`.

Les quatre fichiers ont donc été mis à jour avec un statut « validé et écrit »
sur la base d'une lecture directe du wiki, pas d'une supposition.

### 2.3 Lecture de `Utilisateur:Cywil/Bac à sable` avant écriture (Phase C)

`bin/wiki-get.sh` : page existante (`pageid: 110` confirmé séparément par
`action=query`), contenu vide. Lu avant toute proposition d'écriture,
conformément au garde-fou n°1 de `CLAUDE.md`.

### 2.4 Contrôle de protection avant écriture (Phase C)

`bin/wiki-get.sh --protection "Utilisateur:Cywil/Bac à sable"` : « Aucune
protection native (Lockdown, si actif, ne serait pas visible ici) ».

### 2.5 Résultat de la piste 1 de la Tâche 4a (Phase C)

Voir §1.1 pour le détail. Sortie exacte observée sur la page rendue :
`26x34,20x27`. La piste 1 (`#arraymap` comme normaliseur) fonctionne dès le
premier essai — confirme l'hypothèse posée par `lot-6-suite.md` sans
nécessiter la piste 2 (`#replace`, fonctions de chaîne ParserFunctions) ni la
piste 3 (changement de widget vers `checkboxes`).

---

## 3. Ce qui a échoué ou n'a pas pu être obtenu

Rien n'a échoué techniquement cette session : la seule écriture tentée
(`bin/wiki-put.sh` sur le bac à sable) a réussi au premier essai, la session
étant relancée par précaution juste avant.

Points restés hors du périmètre de cette session, par construction ou par
consigne explicite, pas par échec :

1. **Pistes 2 et 3 de la Tâche 4a non testées.** La piste 1 ayant fonctionné
   du premier coup, les pistes 2 (`#replace`, à tester seulement en cas
   d'échec de la piste 1) et 3 (widget `checkboxes`, nécessite une
   vérification humaine dans le navigateur) n'avaient pas lieu d'être
   essayées, conformément à l'ordre imposé par `lot-6-suite.md`.
2. **4b, 4c, 4d non traités.** Consigne explicite de l'utilisateur : « traite
   la partie 4a uniquement ». `lot-6-suite.md` demande d'attendre validation
   avant 4b — en attente.
3. **Tâches 3, 5 et 6 du lot 6** — non abordées cette session.
4. **Suppression de `lot-6-consolide.md`**, demandée par l'en-tête de
   `lot-6-suite.md` (« Ce fichier remplace `lot-6-consolide.md`... Supprimer
   le précédent »), **non exécutée**. Même traitement que le point analogue
   resté en suspens le 9 août pour `lot-6bis-cadrage.md` : à trancher
   explicitement par l'utilisateur plutôt qu'une suppression décidée
   unilatéralement.

---

## 4. Pour mémoire

Aucun nouveau fichier `diffs/` créé cette session (l'écriture de la Tâche 4a
est un test en bac à sable, hors du formalisme diff/validation utilisé pour
les modifications de modèles/formulaires). Les quatre fichiers existants
(`diffs/2026-08-09-tache1.md`, `diffs/2026-08-09-tache1-etape2.md`,
`diffs/2026-08-09-tache2.md`, `diffs/2026-08-10-tache2-2c.md`) ont vu leur
en-tête « Statut » corrigé pour refléter l'état réel confirmé sur le wiki
(§2.2), et `diffs/2026-08-10-tache2-2c.md` a reçu la note d'explication de
l'écart `Item_description` (§2.1).

---

# Session 2 — corrections `[[]]` vide, Tâche 3, Tâche 6, Organic item multi-parent

**Exécuté le :** 10 août 2026, session Claude Code distincte de la précédente,
compte `Cywil@claude-sgdt`.

La session couvre, dans l'ordre demandé par l'utilisateur : la correction d'un
bug d'affichage (`[[]]` littéral sur paramètre vide) sur les trois modèles
d'items concernés, l'ajout d'une dette (`000J`), l'exécution de la Tâche 3
(audit des directions inverses), l'exécution de la Tâche 6 (consignation des
limites connues, création d'une page dédiée et lien croisé), puis — sur
relecture explicitement redemandée par l'utilisateur avant toute proposition —
la correction du support multi-parent de `Organic item` (modèle et
formulaire).

## 1. Écritures sur le wiki

Sept écritures cette session, toutes par `bin/wiki-put.sh` (aucun besoin de
curl direct en écriture). Protection vérifiée (`--protection`) avant chaque
écriture sur page existante — résultat identique à chaque fois : « Aucune
protection native (Lockdown, si actif, ne serait pas visible ici) ».
`bin/wiki-login.sh` relancé en début de session et une seconde fois juste
avant la phase d'écriture finale (Organic item / Formulaire / page / lien
croisé), conformément à la convention « la session expire entre lecture et
écriture ».

### 1.1 `Modèle:Referenced item` — correction `[[]]` vide

- **Résultat API :** `result: Success`, `pageid: 33`, `oldrevid: 349`,
  `newrevid: 352`.
- **Résumé d'édition exact :**
  `[Lot 6][Correction] Modèle:Referenced item — encadrer Corresponds_to_organic/Supplier/Manufacturer de #if: pour éviter [[]] vide`
- **Diff exact :**
```diff
 ! style="background:#e8f0ff" | Item Organique associé
-| [[{{{Corresponds_to_organic|}}}]]
+| {{#if:{{{Corresponds_to_organic|}}}|[[{{{Corresponds_to_organic|}}}]]}}
 ...
 ! style="background:#e8f0ff" | Fournisseur
-| [[{{{Supplier|}}}]]
+| {{#if:{{{Supplier|}}}|[[{{{Supplier|}}}]]}}
 ...
 ! style="background:#e8f0ff" | Fabricant
-| [[{{{Manufacturer|}}}]]
+| {{#if:{{{Manufacturer|}}}|[[{{{Manufacturer|}}}]]}}
```
- **Contrôle post-écriture :** `bin/wiki-get.sh` relu, diffé contre le fichier
  local écrit — identique.

### 1.2 `Modèle:Organic item` — correction `[[]]` vide (`Realizes_function`)

- **Résultat API :** `result: Success`, `pageid: 58`, `oldrevid: 344`,
  `newrevid: 353`.
- **Résumé d'édition exact :**
  `[Lot 6][Correction] Modèle:Organic item — encadrer Realizes_function de #if: pour éviter [[]] vide`
- **Diff exact :**
```diff
 ! style="background:#e8f0ff" | Réalise la fonction
-| [[{{{Realizes_function|}}}]]
+| {{#if:{{{Realizes_function|}}}|[[{{{Realizes_function|}}}]]}}
```
- **Contrôle post-écriture :** relu, identique.

### 1.3 `Modèle:Physical item` — correction `[[]]` vide (`model_link`, `physical_parent`)

- **Résultat API :** `result: Success`, `pageid: 62`, `oldrevid: 287`,
  `newrevid: 354`.
- **Résumé d'édition exact :**
  `[Lot 6][Correction] Modèle:Physical item — encadrer model_link/physical_parent de #if: pour éviter [[]] vide`
- **Diff exact :**
```diff
 ! style="background:#f2f2f2" | Modèle (Réf. technique)
-| [[{{{model_link|}}}]]
+| {{#if:{{{model_link|}}}|[[{{{model_link|}}}]]}}
 ...
 ! style="background:#e8f0ff" | Installé dans (Parent physique)
-| [[{{{physical_parent|}}}]]
+| {{#if:{{{physical_parent|}}}|[[{{{physical_parent|}}}]]}}
```
- **Contrôle post-écriture :** relu, identique.

### 1.4 `Modèle:Organic item` — `Part_of` multivalué

- **Résultat API :** `result: Success`, `pageid: 58`, `oldrevid: 353`,
  `newrevid: 355`.
- **Résumé d'édition exact :**
  `[Lot 6][Correction] Modèle:Organic item — Part_of multivalué (+sep=,) et affichage en liens`
- **Diff exact :**
```diff
 |Realizes_function={{{Realizes_function|}}}
 |+sep=,
 |Part_of={{{Part_of|}}}
+|+sep=,
 |Connection_gender={{{Connection_gender|}}}
 ...
 ! style="background:#f2f2f2" | Parent organique
-| {{{Part_of|}}}
+| {{#arraymap:{{{Part_of|}}}|,|@@@|[[@@@]]|,&#32;}}
```
- **Contrôle post-écriture :** relu, identique. Vérification fonctionnelle
  détaillée en §2.6.

### 1.5 `Formulaire:Organic item` — champ `Part_of` en `list|tokens`

- **Résultat API :** `result: Success`, `pageid: 59`, `oldrevid: 345`,
  `newrevid: 356`.
- **Résumé d'édition exact :**
  `[Lot 6][Correction] Formulaire:Organic item — champ Part_of en list|tokens, aligné sur Functional item`
- **Diff exact :**
```diff
 ! Parent organique :
-| {{{field|Part_of|input type=combobox|values from category=Organic item}}}
+| {{{field|Part_of|list|delimiter=,|input type=tokens|values from category=Organic item}}}
```
- **Contrôle post-écriture :** relu, identique.

### 1.6 `Limites connues du Système de Gestion de Données Techniques` — création

- **Méthode :** `bin/wiki-put.sh --createonly` (existence vérifiée au
  préalable : `bin/wiki-get.sh` sur ce titre → `ERREUR: The page you
  specified doesn't exist.`).
- **Résultat API :** `result: Success`, `new: true`, `pageid: 144`,
  `oldrevid: 0`, `newrevid: 357`.
- **Résumé d'édition exact :**
  `[Lot 6][Tâche 6] Limites connues du SGDT — création, 13 entrées (dettes, limites de modèle, faits à retenir)`
- **Contenu exact écrit :** intégralité de `pages/Limites_connues_SGDT.draft.txt`
  — intro, lien vers le Récapitulatif, 13 entrées numérotées (dettes connues,
  limites de modèle assumées, et le fait `000J`), note de bas de page datée.
  Contenu élaboré à partir des douze dettes de `lot-6-suite.md` (Tâche 6) plus
  une treizième ajoutée sur demande explicite (pages de fournisseur nues,
  cf. §2.7), après deux retours de l'utilisateur sur le titre (« Limites
  connues... » plutôt que « Dettes techniques... ») et le statut de la page
  (page à part entière, pas une sous-page du Récapitulatif).
- **Contrôle post-écriture :** relu, diff contre le fichier local — seule
  différence : absence de retour à la ligne final côté wiki (MediaWiki le
  supprime à l'écriture), contenu par ailleurs strictement identique.

### 1.7 `Récapitulatif technique du Système de Gestion de Données Techniques` — lien croisé

- **Résultat API :** `result: Success`, `pageid: 27`, `oldrevid: 310`,
  `newrevid: 358`.
- **Résumé d'édition exact :**
  `[Lot 6][Tâche 6] Récapitulatif technique — lien croisé vers Limites connues du SGDT`
- **Diff exact :**
```diff
 ''English version: This page is the technical backbone of the [[System for Technical Data Management]]. It lists all semantic properties, templates, and forms.''
 
+Voir aussi les [[Limites connues du Système de Gestion de Données Techniques]].
+
 == 1. Propriétés Sémantiques (Semantic Properties) ==
```
- **Contrôle post-écriture :** relu, identique. Lien écrit sur une seule ligne
  (leçon de méthode `CLAUDE.md` sur les retours à la ligne dans `[[ ]]`) ;
  contrôlé par `list=backlinks` (curl direct, `wiki-get.sh` ne gère pas cette
  liste) sur `Limites connues du Système de Gestion de Données Techniques` :
  un seul retour, `pageid: 27`, le Récapitulatif — lien bien enregistré dans
  `pagelinks`.

## 2. Résultats de reconnaissance

### 2.1 Relecture fraîche demandée avant toute proposition (Organic item multi-parent)

Sur demande explicite de l'utilisateur (« ne te fie pas aux copies locales »),
relecture en direct de six pages avant tout raisonnement :
- `Modèle:Functional item`, `Modèle:Organic item`, `Modèle:Referenced item`,
  `Modèle:Physical item` : **identiques** aux copies locales (`diff` vide).
- `Formulaire:Organic item` : **divergent** — copie locale datée du 28
  juillet, avant les renommages de la Tâche 2/2C et les ajouts de la
  Tâche 4 :
  - `description`→`Item_description`, `function`→`Realizes_function`,
    `parent`→`Part_of`.
  - Neuf champs de raccord ajoutés côté wiki, absents en local :
    `Connection_gender`, `Thread_designation`, `Nominal_diameter`,
    `Secondary_diameter`, `Connection_standard`, `Fitting_family`,
    `Material`, `External_classification`.
- `Formulaire:Referenced item` : **divergent**, même cause — copie locale du
  28 juillet :
  - `description`→`Item_description`, `organic_link`→`Corresponds_to_organic`,
    `maturity`→`Maturity_level`, `parents`→`Part_of`.
  - Quatre champs ajoutés côté wiki, absents en local : `Max_head`,
    `Supplier`, `Supplier_reference`, `Manufacturer`,
    `Manufacturer_reference` (cinq en réalité, tous listés).

Les deux copies locales de formulaires ont été remplacées par l'état lu à
l'instant (`pages/Formulaire_Organic_item.wikitext.txt`,
`pages/Formulaire_Referenced_item.wikitext.txt`).

### 2.2 Recherche d'une page de dettes existante sur le wiki (Tâche 6)

Deux titres candidats testés directement :
- `Portail:SGDT` → `ERREUR: The page you specified doesn't exist.`
- `Récapitulatif technique SGDT` → `ERREUR: The page you specified doesn't
  exist.` (mauvais titre)

Titre exact retrouvé par recherche dans les rapports locaux :
`Récapitulatif technique du Système de Gestion de Données Techniques`. Lu en
entier (293 lignes) : aucune section de dettes ; la plus proche,
« Ce qui reste à documenter », ne couvre que des réglages `LocalSettings.php`
non exposés par l'API (`$wgNamespacePermissionLockdown`,
`$wgGroupPermissions`, `$smwg*`) — hors périmètre. Conclusion : aucune page ne
recense les dettes, conformément à l'hypothèse de repli de `lot-6-suite.md`
(« si aucune page ne les recense, proposer une page dédiée »).

### 2.3 Audit Tâche 3 — directions inverses, sur wikitexte relu le jour même

- `Modèle:Functional item` (41 lignes) — section « Solutions organiques
  (Comment) », `{{#ask: [[Realizes_function::{{FULLPAGENAME}}]] ...}}` :
  présent.
- `Modèle:Organic item` — section « Implémenté par (Solution technique) »,
  `{{#ask: [[Corresponds_to_organic::{{FULLPAGENAME}}]] ...}}` : présent —
  alors que `lot-6-suite.md` le marquait « à vérifier ». Hypothèse du cadrage
  infirmée à l'exécution : ce réciproque existait déjà avant la session,
  auteur et date non déterminés.
- `Modèle:Referenced item` — section « Exemplaires physiques » : présent,
  déjà confirmé le 9 août (2B).
- BOM aux trois niveaux (`Part_of` → enfants) : présent sur `Functional item`
  (« Sous-fonctions »), `Organic item` (« Sous-systèmes »), `Referenced item`
  (« Composants enfants (BOM) »).

Limite explicite de la méthode : audit fait au niveau du wikitexte source
(`bin/wiki-get.sh` ne renvoie pas de HTML rendu) — confirme que chaque `#ask`
est posé avec la bonne direction de propriété, pas que des données réelles
remontent dans le tableau produit sur une page donnée.

### 2.4 Catégorie `Organic item` — effectifs et données réelles

`bin/wiki-get.sh --category "Organic item"` → 3 pages : `Bidon 220L`,
`Cuve de récupération d'eau`, `Transfert d'eau par vases communicants`.

Wikitexte de chacune lu :
- `Cuve de récupération d'eau` : `Item_ref=000L`,
  `Realizes_function=Stocker l'eau non potable`, pas de `Part_of`.
- `Transfert d'eau par vases communicants` : `Item_ref=000Q`,
  `Realizes_function=Acheminer l'eau au point d'usage`, pas de `Part_of`.
- `Bidon 220L` : `Item_ref=000M`,
  `Realizes_function=Stocker l'eau non potable`,
  `Part_of=Cuve de récupération d'eau, Transfert d'eau par vases
  communicants` — **deux parents déjà saisis en production**, avant la
  correction du modèle (donc jusque-là mal stockés en un seul fait littéral).
  Donnée réelle préexistante, non créée pour ce contrôle.

### 2.5 `Modèle:Organic item` avant correction — constats confirmés

Lecture directe du bloc `#set` : `|+sep=,` (ligne 9) suit immédiatement
`|Realizes_function=...` (ligne 8), pas `|Part_of=...` (ligne 10) — le
`+sep=,` suivant (ligne 13) s'applique à `Thread_designation`. `Part_of` ne
bénéficiait donc d'aucun `+sep=,`. Affichage (ligne 45 avant correction) :
`| {{{Part_of|}}}` — texte brut, sans lien.

`Formulaire:Organic item` (ligne 14 avant correction) :
`{{{field|Part_of|input type=combobox|values from category=Organic
item}}}` — pas de `list`, contrairement à `Realizes_function` juste au-dessus
et à tous les autres champs multivalués du même formulaire.

`Formulaire:Functional item` relu comme référence de parité :
`{{{field|Part_of|list|delimiter=,|input type=tokens|values from
category=Functional item|placeholder=Sélectionner...}}}` — `placeholder=`
non repris dans la correction, hors du périmètre énuméré par l'utilisateur.

### 2.6 Vérification post-écriture demandée : `Part_of` à deux valeurs sur `Bidon 220L`

Demande explicite : confirmer que le stockage produit deux faits distincts et
que l'affichage produit deux liens, sur un item réel.

- **Stockage** (`action=browsebysubject`, curl direct, méthode documentée en
  Tâche 5 leçon 8) sur `Bidon 220L` : propriété `Part_of` avec deux
  `dataitem` de `type: 9` (Page) — `Cuve_de_récupération_d'eau#0##` et
  `Transfert_d'eau_par_vases_communicants#0##`. Pas de chaîne concaténée.
  `_MDAT` de la page à `2026/8/10/15/28/59`, cohérent avec un recalcul SMW
  déclenché par l'édition du modèle un peu plus tôt dans la session.
- **Affichage** (`action=parse&prop=text`, curl direct — hors périmètre de
  `bin/wiki-get.sh` qui ne gère que `prop=wikitext`) sur `Bidon 220L` :
  ligne « Parent organique » du tableau rendu contient deux balises `<a
  href>` distinctes (`Cuve de récupération d'eau`, `Transfert d'eau par
  vases communicants`), séparées par une virgule.

Les deux volets de la vérification demandée sont confirmés, sur donnée réelle
préexistante (§2.4), sans qu'aucune valeur de test n'ait été ajoutée à un item
de production.

### 2.7 Treizième entrée ajoutée aux limites connues, sur consigne de l'utilisateur

Signalée par l'utilisateur (pas trouvée par reconnaissance ce jour) : les
pages créées via la propriété `Supplier` de `Referenced item` (ex. Borde,
Weldom) sont des pages de l'espace principal sans modèle, sans formulaire, ni
catégorie. Consignée telle quelle comme entrée 13, sans vérification
supplémentaire sur le wiki cette session (pas de reconnaissance dédiée
demandée).

## 3. Ce qui a échoué ou n'a pas pu être obtenu

Aucune écriture n'a échoué cette session — les sept ont réussi au premier
essai.

1. **Suppression du brouillon `pages/Dettes_techniques_SGDT.draft.txt`
   (fichier local, pas une page wiki), tentée puis refusée par l'outil :**
   `Permission to use Bash with command rm ... has been denied.` Le fichier
   reste sur disque, remplacé en pratique par
   `pages/Limites_connues_SGDT.draft.txt` (titre corrigé par l'utilisateur) —
   sans conséquence, mais non nettoyé.
2. **Hypothèse de cadrage infirmée (Tâche 3) :** `lot-6-suite.md` marquait
   « organique → référencés qui le fournissent » comme « à vérifier ». L'audit
   a montré que ce réciproque était déjà présent avant la session — pas une
   correction manquante, une case du tableau de cadrage obsolète.
3. **Correction du même bug `[[]]` sur `Organic item`/`Physical item` :**
   d'abord signalée sans être corrigée (vérification seule demandée dans le
   message initial), puis explicitement autorisée et faite dans un message
   suivant — pas un échec, mais une action réalisée en deux temps distincts
   qu'il convient de ne pas présenter comme faite d'un bloc.
4. **Tâche 5 (neuf règles à ajouter à `CLAUDE.md`) non traitée cette
   session** — annoncée comme prochaine étape en fin de session, pas
   commencée.
5. **Suppression de `lot-6-consolide.md`**, déjà en suspens depuis la session
   précédente (demandée par l'en-tête de `lot-6-suite.md`), toujours **non
   tranchée** — non retraitée cette session.

## 4. Pour mémoire

**Nouveaux fichiers `diffs/` créés cette session :**
- `diffs/2026-08-10-correction-if-vides.md` — couvre les trois écritures
  §1.1–1.3 (statut « validé et écrit » avec les numéros de révision).
- `diffs/2026-08-10-organic-item-multiparent.md` — couvre les écritures
  §1.4–1.5.

**`lot-6-suite.md` mis à jour localement (pas d'écriture wiki) :**
- Dette 12 ajoutée (`000J`, trou définitif, non réattribuable).
- Dette 13 ajoutée (pages de fournisseur nues).
- Tâche 3 : tableau corrigé (« à vérifier » → « présent » pour le réciproque
  organique → référencés), note d'audit datée ajoutée.
- Contrôle final corrigé : effectifs 20 fonctionnels / 3 organiques / 2
  référencés / 3 physiques (au lieu de 2 organiques) ; séquence Base 36 de
  `0001` à `000Q` avec le trou connu en `000J` (au lieu de « contiguë de
  `0001` à `000P` »).
- Règle 9 ajoutée à la Tâche 5 (« lire l'état du wiki avant de raisonner, pas
  seulement avant d'écrire »).
- Retour de l'utilisateur sur la Tâche 6 consigné (titre, page à part
  entière, lien croisé bidirectionnel).

**Copies locales `pages/` rafraîchies ou créées cette session :**
`Modele_Referenced_item.txt`, `Modele_Organic_item.txt`,
`Modele_Physical_item.txt` (déjà à jour, réécrites après chaque correction),
`Formulaire_Organic_item.wikitext.txt`, `Formulaire_Referenced_item.wikitext.txt`
(remplacées, étaient périmées depuis le 28 juillet),
`Recapitulatif_technique_SGDT.wikitext.txt` (nouvelle copie locale),
`Limites_connues_SGDT.draft.txt` (contenu final, correspond à la page créée).
